#!/bin/sh

set -e

if [ ! -f /tmp/tailscale ]; then
    arch=`uname -m`
    if [ "$arch" == "mips" ]; then
        wget https://github.com/codding-nepale/openwrt-tailscale-enabler/raw/refs/heads/main/tmp/tailscaled -O /tmp/tailscaled
    fi
    echo "Done!"
fi

/tmp/tailscale "$@"
