#!/bin/sh

set -e

if [ ! -f /tmp/tailscale ]; then
    arch=`uname -m`
    if [ "$arch" == "mips" ]; then
        wget https://github.com/codding-nepale/openwrt-tailscale-enabler/raw/refs/heads/main/tmp/tailscale -O /tmp/tailscale
    fi
    echo "Done!"
fi

/tmp/tailscale "$@"
